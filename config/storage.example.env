# Shared NFS root. Set to wherever the compute node mounts the shared filesystem.
ARTIFACT_NFS_ROOT=/path/to/nfs-root/not-a-waste-paper
ARTIFACT_RELEASE=v0.1

# Fill after the public release is uploaded to a permanent archival service.
ARTIFACT_PUBLIC_BASE_URL=
